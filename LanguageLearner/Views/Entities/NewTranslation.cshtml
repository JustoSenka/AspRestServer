@model NewWordModel

@{
    ViewData["Title"] = "Edit " + Model.WordText;
}

<script>
    window.onbeforeunload = function () {
        return true;
    };
    
    function disableBeforeUnload() {
        window.onbeforeunload = null;
    }
</script>

<div class="container">
    <form method="post">
        <input type="hidden" asp-for="WordID" value="@Model.WordID" />
        <input type="hidden" asp-for="TranslationForID" value="@Model.TranslationForID" />

        <partial name="_AlertMessage" for="@Model" />

        <partial name="_EditWord" for="@Model" />

        @* Update, Delete, Cancel Buttons *@

        <div class="form-group row">
            <div class="col">
                <button onclick="disableBeforeUnload()" type="submit" class="btn btn-success" asp-controller="Entities" asp-action="FinishNewTranslation">Create</button>
            </div>

            <div class="col text-right">
                <a class="btn btn-sm btn-secondary" asp-controller="Entities" asp-action="EditWord" asp-route-id="@Model.TranslationForID">Cancel</a>
            </div>
        </div>
    </form>
</div>
